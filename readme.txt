PUSH SWAP


*FILTRAGE D'INPUT*

J'ai filtre l'input en mettant tous les arguments dans une seule string avec un espace entre chaque join
puis en les splittant en plusieurs elements et checkant chaque element. Si un des elements n'est pas valide, exit directement
J'ai ensuite mis mes valeurs dans ma liste chainee.


*OPERATIONS*

J'ai ensuite implemente les operations suivantes:

-pb: Mettre le premier element de la stack a vers la stack b
-pa: Mettre le premier element de la stack b vers la stack a
-ra: Mettre le premier element de la stack a en fin de stack a;
-rra: Mettre le dernier element de la stack a en debut de stack a;

*ALGORITHME*



J'ai choisi radix